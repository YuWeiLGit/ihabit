<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.ihabit">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.WAKE_LOCK" />

    <application
        android:testOnly="false"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:networkSecurityConfig="@xml/network_security_config"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Ihabit.NoActionBar"
        android:windowSoftInputMode="adjustUnspecified">
        <activity android:name=".activity.ReceiverActivity">
        </activity>
        <activity android:name=".activity.NewPassWordActivity" />
        <activity android:name=".activity.FightActivity" />
        <activity android:name=".activity.BossActivity" />
        <activity android:name=".activity.BlackSmithActivity" />
        <activity android:name=".activity.AddOneTagActivity" />
        <activity android:name=".activity.AssignActivity" />
        <activity android:name=".activity.CharacterActivity" />
        <activity android:name=".activity.LoginActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".activity.AllTasksActivity" />
        <activity android:name=".activity.AddTagActivity" />
        <activity android:name=".activity.AddHabitActivity" />
        <activity android:name=".activity.MainActivity" />
        <receiver android:name=".AlarmReceiver" />
    </application>

</manifest>