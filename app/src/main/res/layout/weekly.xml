<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="wrap_content"
    android:layout_height="wrap_content">

    <TextView
        android:id="@+id/week_box"
        android:layout_width="37dp"
        android:layout_height="22dp"
        android:layout_margin="3dp"
        android:background="@drawable/weekly"
        android:textColor="@color/white"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintHorizontal_bias="0.1"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent" />

    <TextView
        android:id="@+id/showWeeklyText"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="@string/add"
        app:layout_constraintBottom_toBottomOf="@+id/week_box"
        app:layout_constraintEnd_toEndOf="@+id/week_box"
        app:layout_constraintStart_toStartOf="@+id/week_box"
        app:layout_constraintTop_toTopOf="@+id/week_box" />
</androidx.constraintlayout.widget.ConstraintLayout>